<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="utf-8"/>
        <style type="text/css">
            * {
                font-family: "Liberation Serif", Serif;
            }

            @page {
                size: A4 portrait;

                {{#if page.margin}}
                    margin: {{page.margin}};
                {{else}}
                    margin: 1cm 1cm 2cm 3cm;
                {{/if}}
            }
        </style>
    </head>
    <body>
        <div class="page">
            <img src="{{img.url}}" style="
                             {{#if img.width}}width: {{img.width}}{{/if}}
                             {{#if img.height}}height: {{img.height}}{{/if}}
                 " alt="image"/>
        </div>
    </body>
</html>